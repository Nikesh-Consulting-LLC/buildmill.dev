{
  "name": "buildmill-api-tests",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Playwright API test suite for the Build Mill FastAPI service — one spec per router category, plus a summarizing runner.",
  "scripts": {
    "test": "node run-all.mjs",
    "test:pw": "playwright test",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
