fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.8
pydantic-settings>=2.4
PyJWT[crypto]>=2.9
httpx>=0.27
psycopg[binary]>=3.2
# US-87.6: one process-wide connection pool (app/pool.py) replacing a new
# connection per call at 214 call sites.
psycopg-pool>=3.2
litellm>=1.40
paramiko>=3.4
python-multipart>=0.0.9
# <2: mcp 2.0.0 (2026-07) removed `mcp.server.fastmcp`, which factory_mcp.py
# is built on. Environments that already hold a 1.x survive (pip leaves a
# satisfied requirement alone — the VM and dev venvs), but every FRESH install
# resolved 2.0.0 and could not import the app, which is how CI went red on
# 2026-07-30 while every existing machine stayed green. Moving to the 2.x API
# is its own change, not a version-bump side effect.
mcp>=1.10,<2
# US-31.7: .gitignore matching for the changeset guard. gitwildmatch is git's
# own pattern semantics — nested files, negation, directory patterns — and
# hand-rolling a partial version produces refusals developers call wrong.
pathspec>=0.12
# US-81.2: JUnit XML from suite runs is parsed hardened — the report crosses
# our own SSH channel, but a test suite's output is still the org's content.
defusedxml>=0.7
